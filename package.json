{
  "name": "koz-web-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "Проверяемый статический прототип интерфейса KOZ Web",
  "scripts": {
    "test": "node --test",
    "check": "node --check js/app.js && node --test",
    "validate:html": "html-validate index.html"
  },
  "devDependencies": {
    "cheerio": "1.2.0",
    "html-validate": "11.6.2"
  }
}
